name: Publish Package

on:
  push:
    branches:
      - main
    paths:
      - package.json
      - .github/workflows/publish-package.yml
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: publish-package
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node (npm publish)
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Read package metadata
        id: pkg
        run: |
          set -euo pipefail
          PACKAGE_NAME=$(node -p "require('./package.json').name || ''")
          PACKAGE_VERSION=$(node -p "require('./package.json').version || ''")
          OLD_NAME=""
          OLD_VERSION=""
          CHANGED="false"

          if [ -z "$PACKAGE_NAME" ] || [ -z "$PACKAGE_VERSION" ]; then
            echo "package.json must include name and version" >&2
            exit 1
          fi

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            CHANGED="true"
          else
            BEFORE_SHA="${{ github.event.before }}"
            if [ -n "$BEFORE_SHA" ] && git cat-file -e "${BEFORE_SHA}:package.json" 2>/dev/null; then
              OLD_NAME=$(git show "${BEFORE_SHA}:package.json" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{try{const p=JSON.parse(d);console.log(p.name||'');}catch{console.log('')}})")
              OLD_VERSION=$(git show "${BEFORE_SHA}:package.json" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{try{const p=JSON.parse(d);console.log(p.version||'');}catch{console.log('')}})")
            fi

            if [ "$OLD_NAME" != "$PACKAGE_NAME" ] || [ "$OLD_VERSION" != "$PACKAGE_VERSION" ]; then
              CHANGED="true"
            fi
          fi

          echo "name=${PACKAGE_NAME}" >> "$GITHUB_OUTPUT"
          echo "version=${PACKAGE_VERSION}" >> "$GITHUB_OUTPUT"
          echo "old_name=${OLD_NAME}" >> "$GITHUB_OUTPUT"
          echo "old_version=${OLD_VERSION}" >> "$GITHUB_OUTPUT"
          echo "changed=${CHANGED}" >> "$GITHUB_OUTPUT"

      - name: Skip when package metadata is unchanged
        if: ${{ steps.pkg.outputs.changed != 'true' }}
        run: |
          echo "No package name/version change detected."
          echo "Current: ${{ steps.pkg.outputs.name }}@${{ steps.pkg.outputs.version }}"
          echo "Previous: ${{ steps.pkg.outputs.old_name }}@${{ steps.pkg.outputs.old_version }}"

      - name: Check if version already exists on npm
        id: npm-check
        if: ${{ steps.pkg.outputs.changed == 'true' }}
        run: |
          if npm view "${{ steps.pkg.outputs.name }}@${{ steps.pkg.outputs.version }}" version >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Version already published on npm. Skipping."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install dependencies
        if: ${{ steps.pkg.outputs.changed == 'true' && steps.npm-check.outputs.exists != 'true' }}
        run: bun install --frozen-lockfile

      - name: Verify types
        if: ${{ steps.pkg.outputs.changed == 'true' && steps.npm-check.outputs.exists != 'true' }}
        run: bun run typecheck

      - name: Run tests
        if: ${{ steps.pkg.outputs.changed == 'true' && steps.npm-check.outputs.exists != 'true' }}
        run: bun run test

      - name: Build package
        if: ${{ steps.pkg.outputs.changed == 'true' && steps.npm-check.outputs.exists != 'true' }}
        run: bun run build

      - name: Publish to npm
        if: ${{ steps.pkg.outputs.changed == 'true' && steps.npm-check.outputs.exists != 'true' }}
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
